import './PayButton.scss';

const PayButton = () => {
    return (
        <div>
            <button className="btn alert">
                <p className="btn__text">PayButton</p>
            </button>
        </div>
    );
};

export default PayButton;
